{
  "type": "object",
  "title": "extractor configuration",
  "required": [
    "api_url"
  ],
  "properties": {
    "api_url": {
      "type": "string",
      "title": "dataset url",
      "description": "This may be used to download older versions of dataset, otherwise the default URL is used.",
      "propertyOrder": 100,
      "default": "https://earthquake.usgs.gov/fdsnws/event/1/query?format=geojson&starttime=2021-01-08&endtime=2021-01-09"
    }
  }
}
